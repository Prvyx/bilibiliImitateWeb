<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.prvyx.dao.video.CheckVidMapper">
    <select id="checkVid" resultType="string">
        select video_id from video where video_id=#{videoId}
    </select>
</mapper>