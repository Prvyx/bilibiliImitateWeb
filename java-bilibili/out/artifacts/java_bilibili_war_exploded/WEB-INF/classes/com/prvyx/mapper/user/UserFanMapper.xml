<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.prvyx.mapper.user.UserFanMapper">
    <update id="addFan">
        update user set fan_number=fan_number+1
        where user_id=#{userId}
    </update>
    <update id="subFan">
        update user set fan_number=fan_number-1
        where user_id=#{userId}
    </update>
</mapper>